<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
						http://www.springframework.org/schema/beans/spring-beans.xsd
						 http://code.alibabatech.com/schema/dubbo
						  http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- 使用zookeeper注册中心暴露服务地址 即zookeeper的所在服务器ip地址和端口号 -->
	<dubbo:registry protocol="zookeeper" address="${dubbo.registry.address}" check="false" />
	
	<!-- 短信service -->
	<bean id="userService" class="com.msg.service.impl.UserServiceImpl" />
	<bean id="customerService" class="com.msg.service.impl.CustomerServiceImpl" />
	<bean id="messageService" class="com.msg.service.impl.MessageServiceImpl" />

	<!-- 声明需要暴露的服务接口 -->
	<dubbo:service interface="com.msg.service.UserService" ref="userService" version="1.0.0" retries="0" timeout="10000" />
	<dubbo:service interface="com.msg.service.CustomerService" ref="customerService" version="1.0.0" retries="0" timeout="10000" />
	<dubbo:service interface="com.msg.service.MessageService" ref="messageService" version="1.0.0" retries="0" timeout="10000" />

</beans>
